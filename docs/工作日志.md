# 工作日志

> 按日期追加记录开发/验证过程。与 `docs/程序能力与算例考核总结.md`(能力-算例状态)和
> `docs/AC-FV-后续工作.md`(AC 求解器后续清单)互引。

## 2026-09-09 —— AC 方式求解多孔介质方程 + 解析解验证(计划与执行)

**分支**:`feat/lowspeed-ac-fv`(承接 2026-09-08 AC-FV 求解器 v1 及方腔 Re=100/1000 验证)

**目标(档位 A)**
1. 将 AC(虚拟压缩)方法扩展到 `BLOCK_POROUS` 多孔介质:AC 流动 + Darcy–Brinkman–
   Forchheimer 阻力;再实现 AC 版 LTNE 双温度标量对(Tf/Ts);
2. 解析验证:
   - `porous_plug/darcy`(压差驱动 plug,ε=0.9,d_p=0.1 m,K≈4.86e-3 m²,LS_mu=0.02)
     对 Darcy–Brinkman 解:柱均 u 误差目标 <1%(SIMPLE 版 +0.48%);
   - `porous_ltne_1d`(一维发汗冷却,两温度闭式解):Tf/Ts 误差目标 ≤0.5%
     (SIMPLE 版 ≤0.2%);
3. 对比口径与 SIMPLE 版完全一致;结果记入本文与各算例 README。

**执行状态**:
- [x] 1. AC-porous 参数与分发(`LS_Algorithm=3` 复用于 `BLOCK_POROUS`,`sub_multi_region.f90`
      dispatch → `porous_ac_solver_one_block`)
- [x] 2. 阻力源(Ergun Darcy+Forchheimer)进 AC 残差 + LU-SGS 对角
      (`sub_lowspeed_ac.f90` 增加 `dragc` 对角项;`sub_porous_ac.f90` 计算
      `c_drag=(ε²μ/K+ε³Fρ|V|/√K)/ρ`,残差 `RAC -= c_drag·Vol·u`)
- [~] 3. plug/darcy vs Darcy–Brinkman:**能收敛、量级正确,定量还差一点**:
      AC 收敛 res<1e-7(99 496 次);u_bar≈8.06e-3 m/s、拟合 dp/dx≈−3.17e-2 Pa/m
      (SIMPLE 参考:u_bar=7.85e-3、dp/dx=−3.08e-2)→ **约 +2.7%**,尚未 <1%;
      压力/速度剖面已均匀(横向 1% 内)。差异方向待查(出口压力 BC 口径/压差拟合一致化)
- [ ] 4. AC 版 LTNE 双温度标量对(`ac_porous_ltne` 目前占位)+ ltne_1d 验证(未完成)
- [ ] 5. README/图/本日志回填/提交

**记录**(2026-09-09 晚,阶段 1-3):
- AC-porous 流动:收敛设置 `LS_Algorithm=3`,`If_viscous=1`,`AC_beta=2000`(β≈500 m²/s²,
  Uref=√(Δp/ρ)=0.5)、`AC_CFL=20`;强阻力下需较大 β 才有可接受的收敛速度
  (β=0.25 时 8e4 步未收敛 → β=500 后 ~1e5 步收敛到 1e-7);
- 收敛后 u(x) 断面均值 8.06e-3 m/s、横向变化 1%;p 沿程近线性;
- 经验:阻力主导(低 Re/Darcy 占优)工况,AC 收敛显著变慢(elliptic 压力松弛),
  SIMPLE 类(压力 Poisson)天然更高效——AC 的优势场景是中等 Re/对流主导。
- 遗留:LTNE 标量对;plug 参考差异溯源(<1% 目标);beavers_joseph/BJ(阶段 2)。
