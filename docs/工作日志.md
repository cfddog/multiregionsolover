# 工作日志

> 按日期追加记录开发/验证过程。与 `docs/程序能力与算例考核总结.md`(能力-算例状态)和
> `docs/AC-FV-后续工作.md`(AC 求解器后续清单)互引。

## 2026-09-09 —— AC-porous(虚拟压缩法解多孔介质方程):进展与待办

分支 `feat/lowspeed-ac-fv`;承接 2026-09-08 的 AC-FV 求解器 v1 / 槽道 / 方腔验证。
目标(档位 A):把 AC 方法用到 `BLOCK_POROUS`(Darcy–Brinkman–Forchheimer 流动 + LTNE
双温度),并对 `porous_plug/darcy` 与 `porous_ltne_1d` 做解析解验证。

### 进展(已完成)
- [x] **AC-porous 流动接入**:`LS_Algorithm=3` 时 `BLOCK_POROUS` 走 AC
      (`sub_multi_region.f90` dispatch → `porous_ac_solver_one_block`,`src/sub_porous_ac.f90`),
      原 SIMPLE/SIMPLEC 路径与默认值不变;`makefile` 已接入新文件。
- [x] **阻力源入 AC**:Ergun Darcy+Forchheimer 阻力进动量残差
      `RAC(m) -= c_drag·Vol·u_m`,`c_drag=(ε²μ/K+ε³Fρ|V|/√K)/ρ`;
      `src/sub_lowspeed_ac.f90` 增加 `dragc` 模块数组并加入 LU-SGS 对角(2:4),
      保证强阻力下隐式稳定。
- [~] **plug/darcy 验证(能收敛、量级正确,定量未收口)**
      `cases/porous_plug_ac/darcy`(压差 0.25→0 Pa,ε=0.9、d_p=0.1 m、μ=0.02,40×20):
      - 收敛设置:`LS_Algorithm=3`、`If_viscous=1`、`AC_beta=2000`(β≈500 m²/s²)、
        `AC_CFL=20`、`AC_Tol=1e-7` → **99 496 步收敛** res_q≈2.6e-8 / res_m≈1.0e-7;
      - u 断面均值 ≈ **8.06e-3 m/s**(横向变化 ~1%),拟合 dp/dx ≈ **−3.17e-2 Pa/m**,
        p 沿程近线性;
      - SIMPLE 参考:u_bar=7.85e-3、dp/dx=−3.08e-2 → AC **≈ +2.7%**,尚未 <1%。
- [x] **工作日志与文档**:新建本文档 2026-09-09 条目;更新
      `docs/AC-FV-后续工作.md` 第 8–11 条;相关提交:`0c63e12`(AC-porous 流动)、
      `1a1f349`(待办文档)。

### 关键经验
- 阻力主导(低 Re/Darcy 占优)下 AC 收敛显著变慢(elliptic 压力松弛);β 太小(0.25)
  8e4 步不收敛 → **需大 β(≈500)才 ~1e5 步收敛**;SIMPLE(压力 Poisson)在此类问题
  上天然更高效,AC 优势场景为中等 Re/对流主导。
- 收敛后场量横向均匀 1%、p 线性,说明阻力入对角/残差实现正确;差异方向集中在
  出口压力 BC 口径与压差拟合一致化。

### 待办(2026-09-10 起)
- [x] P1:实现 `ac_porous_ltne`(AC 面通量的 Tf 对流-扩散 + hv 源 + `(1−ε)k_s` 骨架导热 +
      骨架面热边界 ghost;原为占位),跑通 `porous_ltne_1d` 两温度闭式解对比
      —— 完成于 2026-09-10,见下方新增条目(401 网格 Tf RMS≈1.22 K / rel≈0.19%);
- [x] P1:plug/darcy 定量 **<1%** 收口——统一与 SIMPLE 完全相同的压差拟合窗口与出口压力
      口径,溯源 +2.7% 差异(怀疑:显式出口动量通量 q=p_out 的边界层误差/拟合行选择);
      —— 完成于 2026-09-11:根因=AC β 配置,80×40 同网格 AC vs SIMPLE <1%(见文末条目);
- [ ] P2:LTNE 热态 plug 与 `porous_plug` 主目录口径对齐;
- [~] P2:beavers_joseph(低速-多孔接口 16)在 `LS_Algorithm=3` 下 AC 双块复测(BJ 解析)
      —— 粗网格复测完成(2026-09-10):能收敛但接口滑移存在 AC 系统性偏高 ~6%(见下方);
- [ ] P3:AC-porous 的 β/CFL 自适应与阻力对角预条件加速;把“阻力主导下 AC 慢于 SIMPLE”
      的对比结论量化后记入考核总结 §方法对比;
- [ ] 提交与 README/图回填(每个待办完成即随算例 README、本日志同步更新并提交)。


## 2026-09-10 —— AC-porous LTNE 落地 + BJ 接口16 AC 双块复测(承接上一条待办)

### AC-porous LTNE(`src/sub_porous_ac.f90::ac_porous_ltne` 由占位→实现)
- **实现思路**:AC 流场收敛后,用 AC 速度场重建 `Fi/Fj/Fk` 面质量流(kg/s,方向约定与
  `porous_face_flux` 一致),随后调用 SIMPLE 多孔侧已验证的
  `porous_boundary_block / porous_energy / porous_energy_Ts`
  (Tf 对流-扩散 + hv 源、Ts 骨架导热 + solid_bc 骨架面 BC),耦合 GS 扫掠
  (100 步窗口 dTf,dTs < 1e-3 K 停),保证与 SIMPLE LTNE 同一离散口径。
- **porous_ltne_1d 闭式解验证**(`cases/porous_ltne_1d/run_ac[_g101|_g201]`,
  `LS_Algorithm=3`,AC 流场 ~4.5k 伪时间步收敛,LTNE ~3–10 万扫掠):

  | nx | Δy(µm) | Tf RMS (K) | rel err | Ts RMS (K) | 能量缺口 |
  |---|---|---|---|---|---|
  | 101 | 100 | 3.97 | 0.66% | 3.69 | 0.98% |
  | 201 | 50  | 2.21 | 0.35% | 2.08 | 0.52% |
  | 401 | 25  | 1.22 | **0.19%** | 1.17 | 0.28% |

  与 SIMPLE 参考(4.19/2.25/1.17 K;0.68/0.36/0.18%)基本一致,一阶收敛趋势相同;
  图 `cases/porous_ltne_1d/validation_ltne_ac{,_101,_201}.png`。
- [x] 待办 8 / AC-FV-后续工作 §8 完成。

### BJ 接口16 AC 双块复测(`cases/beavers_joseph/run_ac`,粗网格 81×21+81×25)
- **方式**:`LS_Algorithm=3` 使 fluid/porous 双块都走 AC;默认时间分支(LU_SGS 单步)内
  `solver_one_block` 每块完成后即调用 `couple_lowspeed_porous_interfaces`
  (块 Gauss–Seidel 排序,`opencfd_ec3d_v1.16a.f90`),`AC_Max_Iter=1` + 3e5 外步
  等效整耦合系统伪时间推进;β≈1.1e-2(压差口径的 ~2 倍)。
- **收敛**:外步残差从 O(1e3)→O(1e-4)(res_q≈3.1e-5/res_m≈2.3e-4,300 000 外步,
  单块每步 1 次 LU-SGS 扫掠;曲线见 `run_ac/residual_history.txt`)。相比 SIMPLE
  (每外步 ~11 次内迭代 + 压力 Poisson)确实显著更慢,印证 §方法对比“阻力/椭圆主导下
  AC 收敛慢于 SIMPLE”。
- **与解析对比**(`pv_bj.py --case run_ac`,x=8/2,粗网格):压力拟合
  dp/dx≈−5.95e-4 Pa/m(fluid/porous 基本一致);u_D≈9.78e-4 vs 解析 9.92e-4(−1.5%);
  剖面 L2≈3.3e-2;界面滑移 u_i≈3.60e-3 vs 解析 3.13e-3(+15%,线性外推对首两层
  敏感,单元层面偏高 ~6%);α_eff≈0.68(有限床 + 粗网格亦非 1.0)。
- **结论**:接口16 在 AC 下可稳定收敛,dp/dx、Darcy 平台与剖面趋势复现 BJ 解析;
  但 AC 仍带已知“速度偏高”系统差(plug/darcy +2.7% 同类,待办 P1/9 收口),
  且粗网格+线性外推放大界面误差;细网格定量收口留待 plug/darcy <1% 问题解决后复核。
- 图:`cases/beavers_joseph/run_ac/validation_bj_ac.png`(与 SIMPLE 粗网格参考
  `run_coarse/validation_bj_coarse.png` 同口径)。

### 代码改动
- `src/sub_porous_ac.f90`:新增 `ac_porous_ltne`(面通量重建 + 复用 SIMPLE 温度扫掠)。
- `src/opencfd_ec3d_v1.16a.f90`:默认时间分支内 `LS_Algorithm=3` 时每块求解后即时
  `couple_lowspeed_porous_interfaces`(块 GS 排序,向后兼容,单块算例为无操作)。

## 2026-09-10 —— 进展汇总与下一步(续)

### 本阶段状态(截至 commit `fee4bab`)
- [x] **AC-porous 流动**:plug/darcy 能收敛、量级正确,定量 **+2.7% 未收口**;
- [x] **AC-porous LTNE**:`ac_porous_ltne` 落地并过 `porous_ltne_1d` 闭式解 ——
      401 网格 Tf RMS=1.22 K / rel≈0.19%(≤0.5% 达成);101/201 一阶收敛趋势与
      SIMPLE 参考一致(0.66%/0.35%);
- [~] **BJ 接口16 AC 双块复测**(粗网格 81×21+81×25):收敛到 res_m≈2.3e-4;
      dp/dx 双块一致、Darcy 平台 −1.5%、剖面 L2≈3%;界面滑移单元级偏高 ~6%
      (外推口径 ~15%),疑似与 plug/darcy 的 AC 速度偏高同一根因;
- [x] **文档/算例/图回填**:`porous_ltne_1d`、`beavers_joseph` 两算例 README、
      `docs/AC-FV-后续工作.md` §8/§10 与本日志、validation png 均已更新并提交。

### 下一步工作(优先级,2026-09-10 起)
- [x] **P1 — plug/darcy <1% 定量收口(阻塞项)** ✅ 2026-09-11:统一与 SIMPLE 完全相同的压差拟合
      窗口与出口压力 BC 口径,定位 AC 速度偏高 +2.7%(候选:压力入口/出口显式边界
      通量的边界层误差、`q=p_out` 口径、拟合行选择)。验收:`u_bar` 与 `dp/dx`
      相对 SIMPLE <1%。(根因=AC β 配置,80×40 同网格验收 <1%,见文末 2026-09-11 条目)
- [ ] **P1′ — BJ 接口偏高溯源与细网格复核**:plug 收口后复核
      `cases/beavers_joseph/run_ac`,确认界面滑移 +6%(单元级)是否同根因;
      随后中网格(nyf=41/nyp=49)复测,目标剖面 L2 与 SIMPLE `run_med` 同级
      (≈4.6e-4)+3%。
- [ ] P2 — **LTNE 热态 plug**:AC-LTNE 版热态 plug,与 `porous_plug` 主目录口径对齐;
- [ ] P2 — **AC-LTNE 收敛加速**:温度 GS 扫掠 ~O(N²)(401 网格需 ~1e5 扫掠),
      试验线迭代/多重网格或 ΔT 自适应松弛;并把 `B%Ts` 写入 `flow3d.dat` 重启
      文件以支持 AC-LTNE 续算;
- [ ] P2 — **AC 接口16 耦合加速**:当前 `AC_Max_Iter=1` + 外步的块 GS 收敛慢;
      试验:(a) 每块内多次扫掠再交换(块 Schwarz),(b) β/CFL 自适应或加大 CFL,
      (c) 双时间步/嵌套外迭代;
- [ ] P3 — 把“阻力/椭圆主导下 AC 慢于 SIMPLE”结论与 BJ 收敛曲线量化后记入
      `docs/程序能力与算例考核总结.md` §方法对比,并同步其“能力-算例状态”表
      (AC-porous LTNE=已验证 / BJ 接口16=已复测待收口);
## 2026-09-11 —— plug/darcy P1 定量收口:根因=β 配置(Uref 误取 LS_U_in),80×40 验收 <1%

承接 2026-09-10 的 P1(阻塞项)。**不改动 SIMPLE**;做了三件事:统一对比度量工具、
隔离实验(E1 channel AC 压力入口、E2 plug h-收敛)、修正 AC 用例 β 配置并复核。

### 根因定位(逐一排除候选)
- **拟合窗口/行选择:排除**。用 `cases/porous_plug/plot_validation/pv_plug_compare.py`
  (统一 VTK 读取 + 固定 0.25–0.85 L 窗口 + 行平均/末行两口径 + 端部诊断)复算存档:
  SIMPLE 40×20:u_bar=7.8471e-3、dp/dx=−3.08037e-2;AC:8.0642e-3、−3.16612e-2
  → 相对 **+2.77%/−2.78%**,跨窗口/行稳定,非度量假象。
- **`q=p_out` 口径/端部显式通量:非主因**。逐格看局部 dp/dx 与端元 p 残差:两种求解器
  相对理想解(两端面钉扎 −Δp/L=−3.125e-2)偏差**方向相反、同为 O(h²) 量级端伪影**
  (SIMPLE −1.4%@40、AC +1.3%@40)。
- **真正根因:AC 运行 β 配置错误**。`β=AC_beta·Uref²`,Uref 取 `lowspeed_inlet_velocity`
  返回值,而 `LS_Inlet_Type=3` 时该值就是 `LS_U_in=0.01`(仅作占位/速度标度) →
  存档运行日志 `beta=0.2`,并非文档假设的 β≈500(需 Uref=√(Δp/ρ)=0.5)。
  小 β 使 AC-Rusanov 连续方程数值耗散(λ≈√β)相对物理项 β·u 过大 → 粗网格稳态解
  受端部伪影偏移,速度偏高。
- **验证**:把 plug AC 的 `LS_U_in` 改为 0.5(物理 BC 不变,仅 β 标度)重跑:
  β=500 后 AC 解即复现理想压差解,且收敛更快(40×20 由 ~99 496 步→5 172 步,
  res_m≈1e-7;80×40 为 10 148 步)。

### E1/E2 隔离实验结论
- E1 `cases/channel_ac/type3_pre`(AC 压力入口 Poiseuille,无阻力):β=1 时已接近
  SIMPLE/解析(Ū 偏差 <0.3%,res_m=8e-6 未完全收敛),确认 β 效应是“粗网格+小 β
  耗散”主导、非多孔阻力耦合问题。
- E2 plug h-收敛(h20/h40/h80,同网格 SIMPLE 与 AC β=500 各跑):

  | 网格 | SIMPLE ū / dpdx | AC(β=500) ū / dpdx | AC−SIMPLE | L_eff(AC) |
  |---|---|---|---|---|
  | 20×10 | 7.517e-3 / −2.900e-2 | 8.099e-3 / −3.1286e-2 | +7.7% | 7.991 m |
  | 40×20 | 7.847e-3 / −3.0804e-2 | 7.958e-3 / −3.1259e-2 | +1.4% | 7.998 m |
  | 80×40 | 7.893e-3 / −3.1188e-2 | 7.909e-3 / −3.1252e-2 | **+0.20%** | 7.999 m |

  AC(β=500) 的 L_eff≡8 m、dp/dx 距理想 ≤0.06%,与网格几乎无关(二阶);SIMPLE
  自下方 ~二阶收敛到理想(8.62→8.12→8.02 m)。40×20 上 AC−SIMPLE ~1.4% 主要即
  SIMPLE 自身粗网格偏差(SIMPLE@40×20 距理想 −1.44%,AC 距理想 −0.02%)。
  **80×40 同网格验收:u_bar +0.20%、dp/dx −0.21% → <1% 达成 ✅**。

### 收口动作(本次提交)
- `cases/porous_plug_ac/darcy/control.ec`:`LS_U_in=0.01→0.5`,重跑并刷新存档输出
  (β=500,u_bar=7.958e-3,dp/dx=−3.1259e-2,5172 步收敛);README 同步修正。
- 新增 `cases/porous_plug_ac/hstudy/`(h20/h40/h80 × SIMPLE/AC β=500 运行 +
  README 收敛表 + 两张对比图),80×40 为 P1 <1% 验收记录。
- 新增统一度量工具 `cases/porous_plug/plot_validation/pv_plug_compare.py`
  (含端部诊断:L_eff、端元 p 残差、内部直线外推面压)。
- 待办更新:`docs/AC-FV-后续工作.md` §9 完成;**P1′ BJ 接口偏高复核**(下一条):
  怀疑与 plug 同因(β 标度)——BJ run_ac 的 `AC_beta=1000`,β=1000·(LS_U_in=0?)²…
  其 β≈? 待按本条目口径复核后再定。
- [ ] 每完成一项即随算例 README、本日志、`docs/AC-FV-后续工作.md` 同步更新并提交。

## 2026-09-11(追加)—— high_low_fluid_800K 网格 code-19 分段交错 + AC 试跑

承接 AC-porous/接口19 线:用 **AC(LS_Algorithm=3)** 在 `cases/high_low_fluid_800K`
网格(4 块,Block1 按 `interface19_phaseA` 改为 BLOCK_POROUS,code-19)上试跑
“高速区 1000 步 → 多孔区收敛 → 回高速,逐步缩短间隔”的
`Iflag_Couple_Scheme=1` 分段交错调度。

### 代码改动(均向后兼容,尚未提交,改动均在工作树)
- `src/sub_multi_region.f90::run_staggered_fluid_porous`
  1. code-19 配对搜索**推广到全部 BLOCK_FLUID 块**(原只查第一个 FLUID;本网格
     界面在 Block3,不是块序第一个 FLUID → 原会误报 no interface);
  2. 多孔段由固定 `porous_solver_one_block`(SIMPLE)改为经 `solver_one_block`
     **按 LS_Algorithm 分派** → `LS_Algorithm=3` 时多孔块走
     `porous_ac_solver_one_block`(AC-LU-SGS 伪时间 + LTNE Ts),SIMPLE 不变。

### 试跑记录(`cases/high_low_fluid_800K/run_ac_staggered`,mpirun -np 1,exit 0)
- 设置:`Kstep_Couple_Comp=1000`、`Niter_Couple_Warm=3`、`Niter_Couple_Outer=12`、
  `Kstep_Couple_Min=20`、`Tol_Couple_Tw=1e-2`;AC:`AC_beta=10`(`LS_U_in=10`
  作 Uref → β≈1000 m²/s²)、`AC_CFL=20`、`AC_Max_Iter=20000`、`AC_Tol=1e-6`。
- 调度正常:气体段 1000→1000→1000→500→250→125→62→31→20→20→20→20 步,
  每轮“等温壁 T_w→提取 q_w/p_w→多孔段 AC+LTNE→返回 T_w”数据流正确,共
  4048 气体步,~2 min,无 NaN。
- 收敛趋势(未达 Tol):max|q_w| 2.09e6→7.6e5 W/m²,T_w 300→306–308 K,
  max|ΔT_w| 逐轮 1.56→0.57 K;最终 `iface_couple.dat`:T_w∈[306.1,308.5] K、
  q_w∈[4.6e5,7.6e5] W/m²、p_w∈[2.67e4,2.77e4] Pa。
- 限制(记录):(a) AC-porous 每段跑满 AC_Max_Iter,res_q≈1.2e6/res_m≈2e4
  未达 AC_Tol —— 本 Block1 顶部退化单元 + 无压差驱动,AC 伪时间收敛慢;
  (b) 沿用 interface19_phaseA 多孔参数(hv=0、骨架无底端热沉),T_w 只能
  “储热式”缓慢逼近气体近壁温 → 外层难收敛(与 staggered_tw 无粘试跑同类)。
- 文档:`run_ac_staggered/README.md`、`docs/AC-FV-后续工作.md` §12。




## 2026-09-11(追加②)—— staggered 分段交错推广到码 11/12/13(低速侧 AC)

将“交错 run”思路从接口 19 推广到:11 可压流体↔固体(CHT)、13 低速流体↔固体(CHT)、
12 可压高速↔低速流体(双流体匹配式块 GS);低速流体相关只按 AC(LS_Algorithm=3)。

### 代码改动(均向后兼容,尚未提交,改动均在工作树)
- `src/opencfd_ec3d_v1.16a.f90`:交错分支改调 `run_staggered_multiregion`。
- `src/sub_multi_region.f90`:
  - 新增 `run_staggered_multiregion`(按块类型组合 11/12/13/19 自动分派);
  - 新增 CHT 驱动 `run_staggered_fluid_solid(nMesh,paircode)`(11/13):外层调度相同
    (暖机+减半),流体侧推进(11=可压 LU-SGS;13=LS-AC 至收敛),固体段全 GS 收敛,
    返回固体侧界面 T_w 判敛;
  - 新增 `run_staggered_highlow`(12):每轮 gas 段推进可压块(暖机+减半),LS 段 AC 至
    收敛,每步经既有 `couple_highlow_fluid_face` 双向 ghost 交换;外层判敛
    `max|ΔT|/|Δp|/|Δu|`(新控制 `Tol_Couple_p/Tol_Couple_u`)。
- `src/sub_lowspeed_ac.f90`:新增 **`ac_lowspeed_energy`**——LS-AC 流场收敛后(LS_k>0)
  由 AC 速度重建 Fi/Fj/Fk 并复用 SIMPLE 的 `lowspeed_energy` 解 U(5) 温度,支撑
  13/12 的温度耦合(此前 LS-AC 只解流场不解温度)。
- `src/sub_modules.f90` / `src/sub_read_parameter.f90`:新增判敛控制与 fp_u 等面数组。

### 试跑记录
- ✅ 12(`cases/high_low_fluid_800K/run_ac_staggered_12`):高速 300→减半+LS-AC 至收敛;
  界面 T≈440–447 K、p≈2.77e4 Pa,外层 dT/dp 度量正常、无 NaN。
- ~ 11(`fluid_solid/run_staggered_11`)、13(`bl_cht/run_staggered_ac13`):调度与各自求解
  段跑通;但这两个既有“自检”网格把跨类界面按物理墙式(bc=2+邻接字段)表示,
  `is_interface_bc` 不命中 → per-step 的 couple_fluid_solid_interfaces 本就不做共轭热
  平衡;真 11/13 共轭验收需以 code-11/13 显式界面编码的网格(待办)。


### 下一步修改建议(按优先级,P0 先行)

1. **P0 — 建真实 code-11/13 共轭验收网格**:`fluid_solid`/`bl_cht` 等既有“自检”几何把
   跨类界面按物理墙式(bc=2+邻接字段)表示,`is_interface_bc` 不命中,per-step
   couple 分支本就不做共轭热平衡。建议:用 `bc3d_interface` 的**显式接口行**(如
   `... 1 2 11` / `... 1 2 13`,或沿用 fvbnd 的负码接口约定)重写/生成几何,使
   `couple_compressible/lowspeed_fluid_solid_face` 与新的 `run_staggered_fluid_solid`
   真正进入共轭分支,再对 11/13 做 T_w/q_w 与热量闭合验收。
2. **P1 — CHT 驱动增加“墙式界面旁路”**:在 `run_staggered_fluid_solid` 里对
   “物理码 2+邻接字段”的界面也实施 code-19 式等温壁 T_w → 提取 q_w → 固体段
   q_w-flux 的显式共轭 ghost(不依赖 couple 过滤)。这需要给
   `solid_solver_one_block` 加**每 GS 扫掠重施加界面 q_w ghost 的模块级 override**
   (否则其内部 `set_solid_ghost_BC` 会用物理 BC 覆盖界面 ghost)。
3. **P1 — LS-AC 温度方程专项考核**:`ac_lowspeed_energy` 目前只在 LS_k>0 时启用、
   且复用 `lowspeed_energy`(SIMPLE 离散)。建议:(a) 造一个带加热壁的 LS-AC 单块算例
   (如给 `channel_ac` 加热入口/等温壁)与 SIMPLE 同网格对比 T 剖面;(b) 考核纯对流
   被动标量(LS_k=0)与 `LS_Scheme=2/3` 高阶修正;(c) 复核非正交网格上 Fi/Fj/Fk
   边界面通量的重构。
4. **P2 — code-12 外层判敛与收敛率**:当前 smoke 只到 3–5 轮、`Tol_Couple_p/u` 为绝对
   量。建议跑更多外轮看 ΔT/Δp/Δu 收敛率;把 p/u 判据改成相对(相对界面 |p|/速度标度)
   或按算例标定;并核对低速 Block1 退化网格对 LS-AC 收敛的影响(配合网格重建)。
5. **P2 — 界面识别与驱动统一**:dispatcher 按“块类型组合”分派,而各 driver 各自扫
   `bc_msg2`(已兼容 is_interface_bc 或 nb1/face1 邻接两种表示)。建议抽一个共享
   `find_cross_pair(nMesh, ...)` 同时用于分派与 driver,消除不一致风险。
6. **P2 — 面拓扑推广**:code-19/11/13 驱动目前限定 j-/i- 共形组合;如需一般面组合
   (i±/j±/k±),可对照各 couple_*_face 的面循环把 wall/q_w ghost 的索引逻辑泛化。
7. **P3 — 重启与多进程**:给固体/骨架 Ts 与 LS-AC 温度增加 `flow3d.dat` 重启读回
   (当前跨进程续算只恢复可压区),并复核 AC 多块缓冲语义(目前按单进程验证)。
8. **P3 — 回归与提交**:重构后重跑 `channel_ac`/`lidcavity_ac`/`porous_plug_ac` 及
   code-19 回归(本会话 code-19 回归结果与存档一致);确认后把本次改动(6 个源文件 +
   4 个 run 目录 + docs)整理成提交。


## P0 执行记录(2026-09-11 追加③)—— code-11/13 真共轭验收(运行时界面提升)

### 做法(不改 bc3d.inp,避免两行续行格式陷阱)
发现把 bc3d.inp 物理码直接改成 11/13 会触发 `check_mesh_multigrid`/转换器对
“接口行再读一行续行”的格式要求(fluid_solid/bl_cht 自检网格按单行墙占位生成,
没有续行)。因此改为**运行时提升**:
- `src/sub_IO.f90::align_interface_to_bc_msg`:对“物理墙码 2 + bc3d_interface 真实
  配对(nb1/face1>0)”的耦合面,按其块类型组合提升为规范接口码(FLUID-SOLID→11,
  LOWSPEED-SOLID→13,FLUID-LOWSPEED→12,FLUID-POROUS→19 等);已是接口码的面不变。
- `src/sub_lowspeed.f90` / `src/sub_lowspeed_ac.f90`:边界/ghost/AC 通量的
  `bc_msg2(ksub)%bc < 0` 判断改为 `is_interface_bc(...)`,使提升后的接口面被正确跳过
  物理墙处理、交由 couple/交错驱动提供 ghost。
- CHT 驱动流段开头补一次 `couple_fluid_solid_interfaces`(首个求解步前 seed 界面 ghost)。

### 验收结果
- ✅ code-11 `cases/fluid_solid/run_conj_11`:couple 共轭触发,外轮 1 T_w≈800 K →
  外轮 2 `max|ΔT_w|=5.0e-3 < Tol(1e-2)` **收敛**,q_w≈30–40 W/m²,无 NaN。
- ~ code-13 `cases/bl_cht/run_conj_ac13`(LS-AC):LS AC 20 步即收敛,T_w 逐轮
  337→387→424 K、q_w≈1e5 W/m² 单调演化(共轭热交换真实),3 轮未达 Tol(需更多
  外轮)。无 NaN。
- 两个 README 均已写入对应 run 目录。


## P1 任务状态记录(2026-09-11 追加④)

### P1-② LS-AC 温度方程专项考核 — ✅ 完成(初步)
- `sub_lowspeed_ac.f90`:能量启用条件 `LS_k>0` → `LS_k>0 .or. LS_T_wall>0`
  (支持 LS_k=0 加热壁纯对流)。
- 新增算例 `cases/channel_ac/{twall_simple,twall_ac,twall_ac_k0}`(入口 u=1,
  上/下等温壁 LS_T_wall=350):
  - SIMPLE(LS_Algorithm=1,LS_k=0.025):1721 次内迭代收敛,res_p≈1e-10,
    T∈[300.0003,349.54] K(壁温 350 被充分逼近);
  - AC(LS_Algorithm=3,同网格/物性):流场 39 370 伪步收敛(res≈1e-7),
    AC 温度 GS 550 sweeps 收敛(max|ΔT|/50=2.6e-4);
  - AC k=0(纯对流,LS_Scheme=3):温度 50 sweeps 即收敛(max|ΔT|=1.6e-8),无 NaN。
- 注:剖面 RMS 定量对比(逐列 T 差值)待补;Fi/Fj/Fk 口径与 SIMPLE `lowspeed_face_flux`
  同为“i/j/k 面用对应笛卡尔速度分量”,已核对一致。

### P1-① CHT “等温壁 T_w+q_w 拆分”模式 — ◐ 已实现(默认关),迭代暂不收敛待标定
- 代码已就位(默认 Iflag_Couple_WallFlux=0 保持 couple 模式不变):
  - 模块级 override 描述 + `solid_solver_one_block` 每 GS 重施加 q_w ghost
    (`apply_solid_qw_override`,j- / j+ 面);
  - `run_staggered_fluid_solid` wfmode 分支:流段等温壁 fill+流体侧提 q_w,
    固体段 q_w-flux override;
  - 新控制 `Iflag_Couple_WallFlux`(namelist/广播)。
- 试跑(`bl_cht/run_conj_ac13/wf`、`fluid_solid/run_conj_11/wf`):调度/固体 GS/提 q_w
  正常、无 NaN,但外层 **发散/振荡**(wf13: T_w 800→~3e3→1.3e4 K;wf11: 1.3e3→0.7e3→1.2e3 K)。
  原因初步:拆分迭代缺欠松弛、初始 q_w≈0(全绝热替身)+符号/单位口径未标定;code-19
  因多孔段天然耗散稳定,而纯固体段 + 强 q_w 的定常迭代更容易振荡。
- 待办:给 wf 外循环加 T_w/q_w 欠松弛(如 α≈0.3–0.5)并按 solid_bc 物理面重新标定
  q_w 符号与 dx;收敛后再与 couple 模式结果互校并开放开关。


## 2026-09-11 收尾——日志与代码保存

本会话覆盖三件事并已全部落盘:① 分段交错推广到码 11/12/13(低速侧 AC);
② P0:code-11/13 真共轭验收(运行时界面提升);③ 两个 P1(P1-② LS-AC 温度专项
基本完成;P1-① wall+q_w 拆分模式已实现、默认关闭,欠松弛标定收敛留待下一轮)。
相关改动(源码 + 算例目录 + README/日志)将在本次一并提交保存,提交说明见 git log。

> 已提交:commit `80dfa9e`(工作树干净)。


## 2026-09-11（追加⑤）—— fluid_solid 2 核并行落地（Pointwise 网格 + 跨进程流固耦合 + 单文件 VTK/SI + 低速 AC）

分支 `feat/lowspeed-ac-fv`。把 `cases/fluid_solid` 重建为 Pointwise 生成的流固共轭算例，
并完成 **2 核跨进程并行**（块1 固体→proc0、块2 流体→proc1），输出**单文件 VTK（SI 量纲）**。

### 1. 算例与网格/边界（Pointwise 生成，未改动）
- 输入：`Mesh3d.x`(PLOT3D unformatted；每块 x,y,z 合并 1 个记录)、`bc3d.inp`(含显式界面码 11
  但**无对接续行**)、`bc3d_interface.inp`(对接关系)、`solid_bc.inp`(块1 J=jmax 面 热流 800 W/m²)、
  `material.in`、`control.ec`；网格单位 mm(`Lscale=0.001`)。
- 块1=固体(67×101×2)，块2=流体(101×201×2)；界面 = **固体 j- 面 ↔ 流体 i+ 面**（共形、旋转对齐）。

### 2. 修复 Pointwise 工作流（源码）
- `sub_convert_inp.f90::convert_inp_inc`：显式界面码 11..19 **不再强读续行**（仅遗留负数码读）；
  `Convert_bc` 同口径 → Pointwise 的 `bc3d.inp` 可正确解析。
- `sub_geometry.f90::check_mesh_multigrid`：同样只对 `bc<0` 跳续行（原会把下一行误读为续行）。
- `sub_IO.f90::read_main_mesh`：`Mesh3d.x` **按文件大小自动识别两种 PLOT3D 布局**
  （每块 x,y,z 合并 vs 三记录），兼容 Pointwise 与仓库既有算例。

### 3. 泛化流固耦合（任意面组合）
- 本地 `couple_compressible_fluid_solid_face` / `couple_lowspeed_fluid_solid_face`：
  由“仅 j 法向”推广到 **6×6 面组合**，切向指标用连接表 `L1/L2/L3` 映射。

### 4. 跨进程流固耦合（2 核核心）
- 新增 `cht_mpi_fluid_solid_face`：两趟交换（流体→固体送 `T_f,k_f,dx_f`；固体算 `T_i`、
  填自身 ghost、回传 `T_i`；流体填自身 ghost），载荷统一**物理单位**（与可压无量纲口径数学等价），
  同时支持可压(`BLOCK_FLUID`)与低速(`BLOCK_LOWSPEED`)流体。
- `couple_fluid_solid_interfaces` 增加远端分支。
- **修复潜伏 bug**：`B_n(nb)` 并非“本地索引”——远程块时返回其**自身进程**内序号(≥1)，
  原 `if(mb<=0)` 永不成立且 `Bn=>Block(mb)` 会指向**错误的本地块**；3 处耦合例程改判 `B_proc(nb) .ne. my_id`。
- **修复死锁**：`update_Ts_buffer_onemesh` 内的 `MPI_Barrier` 在固体 GS 中被单侧调用 → 移除。

### 5. 单文件 VTK + SI 量纲
- `output_vtk`/`output_vtk_merged_flow`：合并文件含**全部块**——固体块 `temperature=Ts`(K)、其余置 0；
  SI 换算仅作用于可压缩流体块；`Iflag_vtk_onefile=1` 时**关闭逐块 `Ts_block_*.vtk`**。
- 控制 `Iflag_vtk_onefile=1`、`Iflag_vtk_SI=1`。

### 6. 低速 AC 复算（超声速 Ma=2 → 低速 80 m/s, 800 K, 绝热壁）
- 块2 改 `BLOCK_LOWSPEED`、`LS_Algorithm=3`(AC)：`LS_U_in=80`、`LS_T_ref=800`、`LS_T_wall=-1`、
  800 K 空气物性(`ρ=0.4413, μ=3.623e-5, k=0.052, Cp=1005`)、`AC_beta=10, AC_CFL=2, AC_CFLv=0.5`；
  固体域与 `bc3d*.inp`/`Mesh3d.x` 不变。
- 2 核求解完成；**1 核 vs 2 核：AC 残差逐位相同，合并 `flow3d.vtk` 逐位相同(max diff = 0)**。

### 验证与产物
- `cases/fluid_solid/`：`Mesh3d.x bc3d.inp bc3d_interface.inp control.ec material.in solid_bc.inp partation.dat plot.pw flow3d.vtk`。
- 运行：`mpirun -np 2 ./opencfd-ec1.16a.out`（1 核删 `partation.dat` 即可）。

### 待办
- 其它跨类对(12/13/16/17/19)的**跨进程**耦合仍未实现（现正确跳过远端邻居，不再误用本地块）。
- AC 在该配置未收到 `AC_Tol`（`res_A≈1e-6`），需调 `AC_beta`/`AC_CFL`；
- VTK 节点坐标仍为网格单位(mm)；`output_Ts` 仍写文本 `Ts_block_*.dat`。

> 本次改动：源码 6 文件 + `cases/fluid_solid` + `docs/工作日志.md`，见本次提交。


## 2026-09-11（追加⑥）—— fluid_solid 改为「低速↔多孔」2 核并行

在追加⑤基础上，把原固体块改为**多孔介质**、界面由 11 改为 **16（low-porous）**，
低速块条件不变，完成 **2 核并行**并与 1 核逐位一致。

### 算例（`cases/fluid_solid`）
- 块1 → `BLOCK_POROUS`：不锈钢骨架 `ρ_s=7900, Cp_s=500, k_s=16.3`，`porous.inp` = ε=0.30、
  dp=1e-3 m、hv=2e6 W/(m³·K)；骨架热边界沿用 `solid_bc.inp`(J=jmax 面 热流 800 W/m²)。
- 冷却剂：`j=101` 进入（bc=5 入口，法向 ≈ +y），**300 K、G=2 kg/(m²·s)** → U=G/ρ=4.532 m/s；
  出口为界面(16)。低速块(块2)保持 80 m/s、800 K、绝热壁、`LS_Algorithm=3`(AC)。
- `bc3d.inp` 界面码 11→16（用户修改）；`Mesh3d.x`/`bc3d_interface.inp` 未改动。

### 代码改动
- **多孔冷却剂专用入口**：新增 `Porous_U_in/Porous_V_in/Porous_W_in/Porous_T_in`（namelist/
  `rpara(69..72)`）；`porous_inlet_velocity`、`porous_ac_solver_one_block` 与 AC 幽灵/多孔幽灵入口温度
  按之覆盖，解决「多孔与低速共用 `LS_U_in/LS_T_ref`」的冲突。
- **泛化低速-多孔耦合** `couple_lowspeed_porous_interfaces`：由「正对面」推广到**任意 6×6 面组合**
  （连接表 `L1/L2/L3` 映射），并**新增跨进程 MPI 交换**（`MPI_Sendrecv`，交换 `U(1:5),p`；
  按接收方指标空间打包）。
- **耦合节奏统一**：移除主程序块循环内的 `couple_lowspeed_porous_interfaces` 调用（否则 1 进程
  每步调用 3 次、N 进程 2 次，导致 1/2 核结果差异 ~40 K）；改为每步末统一耦合一次。

### 验证（2 核 vs 1 核）
- 无 NaN/发散；多孔 AC(β=205)+LTNE 收敛；多孔 Tf∈[302.5,795] K、骨架 Ts∈[300,322] K。
- **合并 `flow3d.vtk` 1 核 vs 2 核逐位相同（max diff = 0）**；低速块 T∈[795,800] K、
  多孔块 Tf 302.5–795 K（SI）。
- 结论：**耦合运行稳定，未启用 staggered**（`run_staggered_multiregion` 目前尚未支持 16/low-porous）。

### 待办
- `run_staggered_multiregion` 增加 low-porous(16) 分派（若需分段交错）；
- 多孔块输出的仍是流体相 Tf（骨架 Ts 另写 `Ts_block_*.dat`）；
- AC 大迭代预算下每步较慢（`AC_Max_Iter`/`Porous_Max_Iter`），可按算例调小。

> 本次改动：源码 6 文件 + `cases/fluid_solid` + `docs/工作日志.md`。


## 2026-09-12 —— bc3d.inp 由 bc3d_interface.inp 自动生成（单一来源）

### 背景
多块算例需同时维护 `bc3d.inp`（物理边界 + 接口码）与 `bc3d_interface.inp`（对接关系）：
接口码（11/13/16/19…）其实由 `material.in` 的块类型组合唯一决定，属冗余；而两者格式约定不同
（`bc3d.inp` 仅对 `bc<0` 读续行，`bc3d_interface.inp` 对 `is_interface_bc` 读续行），手工同步易错
（如 `beavers_joseph` 的 `bc3d.inp` 在正码 16 后多写续行，会被 `convert_inp_inc` 误读）。

### 做法（`src/sub_convert_inp.f90::prepare_bc3d_input`）
- 启动时（`ID=0`，`check_mesh_multigrid` 之前）调用：
  - 无 `bc3d_interface.inp` → 直接返回（完全向后兼容）；
  - 无 `bc3d.inp` → 由 `bc3d_interface.inp` + `material.in` **生成** `bc3d.inp`：
    跨类面写规范接口码（FLUID-SOLID→11、FLUID-LOWSPEED→12、LOWSPEED-SOLID→13、
    LOWSPEED-POROUS→16、SOLID-POROUS→17、FLUID-POROUS→19），同类面写 `BC_Inner(-1)`；
    正码不写续行、负码保留续行（与 `convert_inp_inc` 读取口径一致）。
  - 有 `bc3d.inp` → **校验**：面区间按 `Convert_bc` 口径归一（`abs`+`min/max`，故 `-1/-2`≡`1/2`），
    对接面的码须等于块类型推出的规范码（或占位墙码 2）。合理→沿用用户文件；
    不合理或无法解析→备份为 `bc3d.inp.bak` 后重生成（**物理面码从备份回读保留**）。
- 新增开关 `Iflag_bc_check`（`control.ec`，默认 1）：`0`=关闭（旧行为）；`1`=自动生成/校验；
  `2`=严格（不一致则 `stop`）。

### 验证
- `fluid_solid`：删 `bc3d.inp`→生成接口码 16；保留正确文件→“consistent, keep it”；
  篡改 16→11→检出并重生成 16（入口码 5 保留）。
- `bl_cht`：删 `bc3d.inp`→生成 13（LOWSPEED-SOLID）。
- `beavers_joseph`：原 `bc3d.inp`（正码后带续行，旧解析器会误读）被判定不可解析→
  备份+重生成正确文件，运行正常（**顺带修复该遗留格式陷阱**）。
- 注：`bl_cht` 在当前分支运行会在 `couple_lowspeed_fluid_solid_face` 段错误——用原文件同样复现，
  与本次改动无关（既有 bug，另案处理）。


## 2026-09-13（追加）—— BJ（接口 16）AC 复跑：AC 明显差于既有 SIMPLE，需查因

### 背景
本轮考核用 **AC**（`LS_Algorithm=3`）复跑 Beavers--Joseph 两块算例（接口码 16：低速块 + 多孔块），
并做 `ε=0.3/0.6/0.9` 孔隙率扫描。复跑后出现**异常**：AC 结果明显差于仓库既有 SIMPLE 结果。

### 结果对比（x=Lx/2）

| 求解器 | 网格 | u(y) L2/Linf | 界面滑移 u_i 误差 | BJ α_eff | dp/dx 双块一致 | Darcy uD 误差 |
|---|---|---|---|---|---|---|
| **SIMPLE**（README 中网格） | 81×41 + 81×49 | **4.6e-4 / 9.7e-4** | +0.30% | 0.925 | — | ~2% |
| AC（`run_ac`，ε=0.8） | 81×21 + 81×25 | 3.3e-2 / 7.8e-2 | +14.8% | 0.68 | ✓ | −1.5% |
| AC（本次扫描 ε=0.3/0.6/0.9） | 81×21 + 81×25 | 0.41 / 0.21 / 0.21 | — | — | — | — |
| AC（加密 161×81 + 161×97） | 运行至 t≈1/3 超时未收敛 | 1.01 / 0.79 / 0.23 | — | — | — | — |

（本次 AC 扫描用 `AC_Max_Iter=1` 的 block-Jacobi 外步、`t_end=2e5~3e5`；加密网格上外迭代残差停在 ~0.1--0.18，`timeout 5400` 到点终止。）

### 观察与疑点
- **SIMPLE 中网格 L2≈1e-4（与解析几乎重合、近二阶收敛）；AC 同问题 L2≈1e-2，本次 ε 扫描更达 0.2--0.4** ——
  AC 比 SIMPLE 差 **2--3 个量级**，与“同一套离散、仅压力--速度耦合不同”的预期不符，判为异常。
- AC 界面滑移**系统性偏高**（`u_i` +15%，`α_eff` 0.68 vs 理论 1.0）；同现象见 `porous_plug_ac/darcy`（AC 速度 +2.7%）。
- 与前两条记录（工作日志 2026-09-09/10、`docs/AC-FV-后续工作.md` §9）一致：低速--多孔界面在 AC 下精度不足。

### 可能原因（待查，按优先级）
1. **界面 ghost 口径与 AC 面速度重建不自洽**：`couple_lowspeed_porous_interfaces` 直接拷贝 `U(1..5)`、`B%p`，
   而 AC（`sub_lowspeed_ac.f90`/`sub_porous_ac.f90`）的界面通量由 `XW`+`Fi/Fj/Fk` 重建；SIMPLE 与 AC 的
   face-velocity/压力修正口径不同，界面剪应力与滑移因此有系统偏差。
2. **收敛充分性**：SIMPLE 每外层步内迭代到 `1e-6`、外层 250 步；AC 用 `AC_Max_Iter=1` 的 block-Jacobi，
   残差 ~3e-5 即停（本次 ε 扫描甚至停在 0.1--0.18）→ 等效收敛度不足。
3. **`AC_beta` 标定**：`run_ac` 取 `AC_beta=1000`（β≈1.1e-2）；低速--多孔速度量级很小，β 过小会拖慢/畸变伪时间推进。
4. 粗网格下一阶迎风的数值耗散与 AC 低耗散特性差异。

### 结论与待办
- 本轮 BJ 以 **SIMPLE 的中网格收敛解**为准；AC 结果列为 `待改进`（报告 §3.7 已如实记录，不再进一步加密）。
- 待办：统一 AC 与 SIMPLE 的界面 face-velocity/压力修正口径；对低速--多孔界面加隐式/欠松弛或提高内迭代；
  在**同网格**上复核 AC 能否达到 SIMPLE 精度。在此之前，低速--多孔耦合类验证**优先用 SIMPLE**。

## 2026-09-15 —— 多块 AC 耦合两个真根因定位与修复（界面通量单值共享 + 耦合压力锚定）

分支状态：工作树（基线 tag `v1.16a-baseline-orthogonal` / `ceff5d1`），**未提交**。
改动：`src/sub_lowspeed_ac.f90`(+383)、`src/sub_update_buffer_mpi.f90`(+34/−6)。

### 背景
为把 AC 推广到低速/多孔多块与 2D T 形管、3D 方管，先清“多块 AC 不收敛”的真因。
此前怀疑的旋转界面、角点（`Umessage_corner`）、初场、`AC_beta` 标定经隔离后逐个排除。

### 定位过程（关键诊断实验）
1. **2 块槽道隔离算例** `cases/channel_ac_2blk`：把 `channel_ac` 在 x=4 切两块（唯一
   无旋转共形 `-1` 界面，80+80 单元，与单块同分辨率）。读入/界面/网格质量全部正常，
   两侧界面质量通量 0.30591 ↔ 0.30274 一致；但它与**单块** `channel_ac` 用**同一份**
   control.ec 跑出的 res_q 曲线**几乎逐位相同**（step2 4.0875527947929440；step150
   0.54757462513170219 vs …619）⇒ “降到 ~0.17 平台后回升”**与块界面无关**。
2. **对照实验**：单块用 `channel_ac` **原始** control.ec（`AC_Max_Iter=60000, t_end=1`）
   收敛：`iterations=39370, res_q=9.9997e-8`，`u_max=1.49546`（解析 1.5）✅。而先前被
   sed 成 `AC_Max_Iter=1` 时收敛判据 `iter>=20 .and. rq<AC_Tol` **永不成立** ⇒ 此前多块
   结论均建立在错误设置上（已纠正：本仓库既有范式是 `AC_Max_Iter=1` + 海量外步，见 BJ；
   但内步数必须足以让 AC_Tol 能触发，或改回 `AC_Max_Iter` 大 + `t_end=1` 单外步）。
3. **数学根因①（守恒缺陷）**：`ac_flux_rusanov` 耗散项 `−0.5*lam*(qR−qL)` 中
   `lam=max(|unL|+cL,|unR|+cR)` 对法向是**偶函数**，故 `F(−n) ≠ −F(n)`，差值恰为
   `lam*(qB−qA)`；而界面两侧调用传入的 (L,R) 状态**顺序相同**（都是 qA→qB）、只有法向
   相反 ⇒ 两侧界面通量**不抵消**，每个界面凭空产生/吸收 `lam·Δq·A` 的质量与动量。
   （此前“两侧等量反号、必然守恒”的推断是错的。）
4. **数学根因②（压力锚定）**：`ac_have_pressure_bc` 按**单块**判断，只有速度入口的
   入口块被判为“无压力边界”→ 每个伪步 `ac_zero_mean_p` 减掉**本块自身**均值；在耦合
   系统里这等于给界面**人为加一个压力跳变**（邻居绝对压力被减掉本块均值），产生持续
   动量源、让无锚定的压力模态增长 ⇒ 入口块典型症状“**先收敛（1e-7）后发散**”。
   出口块有出口压强锚定 ⇒ 一直正常收敛（这解释 blk2/blk4 正常而 blk1/blk3 坏）。

### 代码改动
- `src/sub_lowspeed_ac.f90`：
  - `lows_ac_work` 新增共享通量注册表 `FSH(4,nx,ny,nz,nifc)` 与 `ac_ifc_own/ks/nb`；
  - 新增 `ac_build_ifc_registry`（扫描同类 LOWSPEED↔LOWSPEED 界面，owner=块号较小者）、
    `ac_ifc_find`（`(mBlock,ksub)→(id,is_owner)`，**配对不上 id=0 自动退回原双侧通量**）、
    `ac_ifc_src_cell`（用与 `Umessage_send_mpi` **完全相同**的 `L1/L2/L3 + P(±)` 约定把
    对侧 ghost 点映射到 owner 单元 ⇒ **旋转界面也一致**）、`ac_ifc_flux_own`（算一次、
    写 `FSH`、`RAC(own) -= F·A`）、`ac_ifc_flux_nb`（只读 `FSH`、`RAC(own) += F·A`）、
    `ac_ifc_face`（逐面分派，含 fallback）、`ac_interface_fluxes`（取代原 6 段 `ifc(1..6)`）；
  - `ac_have_pressure_bc` 增加**网格级**扫描：串联同类低速块中任一有压力边界 ⇒ 全体
    `have_pbc=.true.`，不再各自去均值。
- `src/sub_update_buffer_mpi.f90`（+34/−6，前一轮）：`Umessage_send/recv_mpi` 去掉块类型
  过滤（`bc<0` 即交换；跨类面 11–19 仍归 `couple_*`，不进缓冲交换），低速对额外在同进程
  直拷 `B%p`（含 L1/L2/L3 映射）。该文件为 **CRLF**，补丁需字节级处理以保持换行。

### 验收（`AC_Max_Iter=60000, t_end=1.d0, AC_Tol=1e-7, flux=1`）

| 算例 / 块 | 修复前 | 修复后 |
|---|---|---|
| `channel_ac_2blk` blk1（owner，无压力边界） | 60000 步 res_q=**6.4e-2** ✗（18000 步曾到 1.09e-7 后回涨） | 22570 步 **9.998e-8** ✓ |
| `channel_ac_2blk` blk2 | 18947 步 9.5e-8 ✓ | 18947 步 **8.73e-8** ✓ |
| `channel_ac_4blk`（2×2；4 界面在内部点 (4,0.5) 交汇＝**角点构型**）blk1 / blk2 / blk3 | blk1 7.7e-2 ✗、blk3 1.4e-1 ✗ | 2799 步 **7.44e-8** / 4131 步 **9.96e-8** / 59520 步 **1.000e-7** ✓ |
| `channel_ac_4blk` blk4 | — | 撞内迭代上限 60001 步 **8.32e-7**（1.4e-6→8.3e-7 仍在降） |

- 2 块收敛解 `u_max = 1.4897 (blk1) / 1.4942 (blk2)`，对比解析 1.5、单块基准 1.49546 ✓。
- **结论：2×2 四块角点构型可收敛，与 2 块/单块同量级，未发现角点病态**（4 块甚至更快：
  blk1 2799 步 vs 2 块 22570 步，说明角点加快了耦合信息传播）。

### 遗留（须先解决才能做文献定量对比）
- [ ] **VTK 单元场极值不正常**：2 块 blk2 `u_min=−2.72`；4 块 blk4 `u_min=−14.4, u_max=2.11`，
  而残差均 ~1e-7。需先确认是 `flow3d_block_*.vtk` **写出口径/场序/是否只写内点/块间缓冲
  是否复用/是否写最终收敛态**的问题，还是真实角点（blk4 未完全收敛）现象；用单块已知
  解析剖面自校验写出路径。
- [ ] 4 块 blk4 提高 `AC_Max_Iter`（如 2e5）或跑 2–3 个外步，压到 1e-7。
- [ ] **`cases/2D_T_tube`**（4 块共形、含 i↔j 旋转对接与三界面交汇）与 **3D 方管**用同一
  设置重跑；参考文献数据需用户指定（仓库内**无** T 形管/方管参考数据）。
- [ ] `Ts_send/recv_mpi` 过滤条件仍为 `is_interface_bc`（含 11–19），固/多孔温度缓冲跑在
  跨类共轭面上、与 `couple_*` 重复写 ghost；应按同类面（-1）收窄，并对
  `fluid_solid`(11)/`bl_cht`(13)/`beavers_joseph`(16) 回归。

### 新增/变更算例与脚本
- 新增 `cases/channel_ac_2blk/`、`cases/channel_ac_4blk/`（`gen_case.py` 直接生成
  `Mesh3d.x` + `bc3d.inp`/`bc3d_interface.inp`/`material.in`，各配 README）；
- 对照运行目录：`cases/channel_ac/ctrl_single/`（同 2 块设置）、
  `cases/channel_ac/ctrl_orig/`（原始设置基准，已收敛 `res_q=1e-7`, `u_max=1.49546`）；
- 运行脚本：`/tmp/ac_tests.sh`、`/tmp/ac_orig.sh`、`/tmp/ac_correct.sh`。


### 补充（同日 2）：真正的主因 = 多块调度（冻结界面），最终验证到机器精度

上一节列的"两个根因"中，①界面通量共享（守恒）成立且必要；但"块1 先收敛后发散"与
**邻块首列异常**的主因是**块间调度**：`AC_Max_Iter=60000, t_end=1` 让**每个块单独迭代到
收敛，期间界面通量被冻结**（`FSH` 只在别的块运行时由它上一次伪步写入，本块整个收敛过程
不再刷新），邻块首列被锁进**假解**（残差到 1e-7，但解非物理）。仓库原有范式
（`AC_Max_Iter` 小 + `t_end` 大 ⇒ 各块每外步同步推进、界面每伪步刷新）才是多块 AC 的正确用法。

#### 判定链（可复现）
1. **先排除 VTK 口径**：用 `POINTS` 反推单元中心校验 `flow3d_block_*.vtk` ——
   `DIMENSIONS 81 41 2`、`CELL_DATA=3200`、单元序 i 最快且只写内点、块坐标
   x∈[0,4]/[4,8] **全部正确** ⇒ 输出路径没问题。
2. **异常定位**：非物理单元**全部落在邻块 `i=1` 列（紧贴界面）**：2 块 blk2 的 `u(y)`
   整列反号（i=1 均值 −1.52、i=2 +0.31、i=3 +0.90 ⇒ 3 个单元内恢复），且中心处
   `u(i=1) ≈ −u(i=2)`（±0.72）—— 这是"界面面通量≈0（或被冻结）"的特征。
3. **打开诊断**（`sub_lowspeed_ac.f90` 的 `ac_dbg`，默认 0）：owner 写
   `FSH(·,80,1,1,id=1)=(0.691,−0.019,−0.003,0)`，邻块 `map=(80,1,1)`、`FSHread` **逐位相同**
   ⇒ **`ac_ifc_src_cell` 映射与共享读写完全正确**（`L1,L2,L3=1,2,3` 全正；手算与实测一致）。
4. **`U(1)` 存的是 ρ（常数）**，AC 压力在 `B%p`；`sub_update_buffer_mpi.f90` 的同进程
   低速对上 `B%p` 已单独交换（同式 `i1=ks+ka(L)·P` 映射）⇒ 压力 ghost 亦正常。
5. **决定性实验（只改设置）**：`AC_Max_Iter=20, t_end=200` ⇒ 邻块 i=1 列立即变物理
   （Σu=+32.6、min=+0.058、无负速度）；延长到 `t_end=3000` ⇒ **所有块残差降到 1e-14 量级**。

#### 最终验证（`AC_Max_Iter=20, t_end=3000, Kstep_save=1000`，共享通量+网格级压力锚定）

| 算例 | 残差 res_q（各块） | u_max（各块） | 其它 |
|---|---|---|---|
| `channel_ac_2blk` | 4.8e-11 / 1.3e-11 | 1.49439 / **1.49546** | 界面两侧 Σu 39.84586 ↔ 39.79611（差 0.12%）；出口 Σu=40.00000（解析）|
| `channel_ac_4blk` | 8.5e-14 / 2.0e-14 / 7.5e-14 / 1.8e-14 | 1.49503 / 1.49561 / **1.49503** / **1.49561** | 上/下镜像**严格对称**；**0 个负速度单元** |

- 单块基准 `cases/channel_ac/ctrl_orig` 为 `u_max=1.49546`；两块/四块结果与之逐位一致
  （2 块 blk2 = 1.49546）⇒ **多块无精度损失**。
- **4 块角点构型（4 界面在内部点 (4,0.5) 交汇）确认无角点病态**：残差最低、镜像对称、
  `u_max` 与解析/单块一致。

#### 更正与教训
- ❌ 上一节"`AC_Max_Iter` 必须取大（60000）"**只对单块的收敛判据触发成立，对多块是错的**；
  多块必须"小内迭代 + 多外步"。已改写 `channel_ac_2blk/4blk` 的 `control.ec` 与 README。
- 残差 1e-7 **不等于**解正确：冻结界面下邻块首列的"自洽假平衡"能满足 max 残差判据。
  **多块验收必须同时看界面两侧剖面/通量一致性与场量极值**（本次即由此发现）。
- 冻结界面的历史结果（`run.log`、`run_fix.log` 与旧 VTK）保留为反例。

#### T 形管（`cases/2D_T_tube`）现状：**能加载能跑，但当前设置不收敛**（2026-09-15 更正）

**先更正一处文件职责误解（重要）**：`bc3d_interface.inp` 只用于**跨类型** block 之间的
对接关系；**同类块**（同一 BLOCK 类型）的对接与物理边界都在 `bc3d.inp` 里——内边界用负码，
连接号/各维正负标记在 `kb/ke` 列，`sub_convert_inp.f90::Convert_bc` 自动求出
`ib1..ke1/nb1/face1/L1..L3` 并写入 `bc3d.inc`。实测：把 `channel_ac_2blk` 的
`bc3d_interface.inp` 删掉后，程序打印 `bc3d_interface.inp not found, skip interface`，
但**同类界面仍被正确解析**（`AC: shared same-class interfaces = 1`）并正常收敛 ✓。
⇒ 原先"T 管接口文件不合法 ⇒ 不能跑"的判断**作废**；同类块算例不需要该文件。
（本次已删除 `channel_ac_2blk|4blk`、`2D_T_tube` 中多余的 `bc3d_interface.inp/.inc`——
这三个算例都是单一块类型；删除后均实测"not found, skip interface"但仍正确解析同类界面，
2/4 块槽道结果**逐位不变**。）

**实测**（`cases/2D_T_tube/run_probe.log`，`t_end=100`）：4 块网格读入正常
（节点 61×61 / 61×101 / 61×101 / 101×61，`Num_Cell=21600`）、**3 个同类界面全部解析**
（`shared same-class interfaces = 3`）、跑完 100 外步无报错并写出 VTK；但
`res_q≈3.0e2`、`res_m≈7.7e1`（同阶段槽道算例为 1e-2 量级），`u_min≈−3.8`（blk1）、
`−7.5`（blk4），数百个 `u<0` 单元 ⇒ **算例设置/几何层面未收口**：
需逐面核对 `bc3d.inp` 的物理边界码与出口压力锚点、检查 T 形交汇处 3 个界面的角点处理，
以及调参（本算例 `AC_CFL=2/AC_CFLv=0.1` 远小于槽道 20，且用 `AC_Flux=3` AUSM+）。
3D 方管算例仓库内**不存在**，且无文献参考数据（需用户指定）。

## 2026-09-16 —— 圆柱绕流（**半域+对称**）标模考核：AC Re=40 阻力 vs Rogers；无粘 Cp 解析解验证

承接 2026-09-15（2D T 形管多块 AC 仍发散）：本轮**换一个单块、外流、同时有解析解与公认
文献的算例**——上半圆柱 + 对称面 y=0——用来隔离"AC 求解器本身"的问题。

新增算例 `cases/cylinder_re40_half/`（README、`gen_case.py`、`postprocess.py`、
`verify_cp_inviscid.py`、`diag_wall.py`，运行子目录 `inviscid/ g2_re40/ r10_re40/
simple_re40/ inv_ausm/ inv_b10/`）。

### 1. 设置（尽量与 Rogers & Kwak 同构）
- 单块极坐标 O 网格：i 径向（i=1 壁面 r=0.5D、i=nx 远场）、j 周向（j=1 → θ=0 即 +x 轴、
  j=ny → θ=π 即 −x 轴）、k 一层（准 2D）；网格 = 节点 121×81×2（单元 120×80），
  R_out=25D，壁面首层 dr1=0.0138D（tanh 聚集）；
- BC：i=1 `2`(壁) ＋ i=nx 上游 1/4 弧 `5`(给速度 U=1)、下游 1/4 弧 `6`(定压 p=0)
  ＋ j=1/j=ny 与 k 面 `3`(对称)；进出流分界取在 **θ=π/2**（该处来流与边界相切）——
  与 Rogers 原文 "where fluid was entering the domain, the velocity was held constant,
  and where fluid was leaving the domain, the static pressure was held constant" 一致；
- 无量纲 D=ρ=U=1、LS_mu=0.025（Re_D=40）；AC：`LS_Algorithm=3, AC_beta=1, AC_CFL=20,
  AC_Max_Iter=200000, AC_Tol=1e-7, t_end=1`（单块正确范式）。

### 2. Re=40 粘性结果与文献对比（Rogers Table 4 已从 NTRS 下载原文逐行核对）

| 量 | Rogers(Present) | Dennis-Chang | Takami-Keller | Tuann-Olson | 本文 120×80 | 本文 160×120 |
|---|---|---|---|---|---|---|
| Cd | **1.549** | 1.522 | 1.536 | 1.675 | 1.5706 | **1.5536** |
| Cd_p | 1.011 | 0.998 | — | 1.095 | 1.0152 | **1.0086** |
| Cd_f | 0.538 | 0.524 | — | 0.580 | 0.5553 | 0.5451 |
| L_sep/D（自后缘）| 2.29 | 2.35 | 2.32 | 2.1 | 2.07 | 2.137 |
| θ_sep（自后驻点）| 53.0° | 53.8° | 53.6° | 54.8° | 54.3° | 54.2° |
| Cp_front | 1.147 | 1.144 | 1.141 | 1.312 | 1.1544 | 1.1593 |
| −Cp_rear | 0.536 | 0.509 | 0.512 | 0.543 | 0.512 | 0.502 |

- **两套独立阻力口径互相印证**：壁面积分（压力+摩擦）与**远场圆弧 x 动量平衡**
  （半域两条对称射线因 v=0、∂u/∂y=∂v/∂x=0 而不贡献 x 动量 ⇒ 可用弧上积分直接给阻力）
  吻合到 0.0~0.3%；控制面取 r≈2/5/12D 时 1.5862/1.5844/1.5792 单调趋近远场值 ⇒ 阻力可信。
- 网格 120×80→160×120：Cd 1.5706→1.5536（−1.1%）、Cd_f 0.5553→0.5451；剪切口径的
  离散不确定度（1 阶 vs 2 阶）由 1.3% 收窄到 0.8% ⇒ 极限 ≈1.54~1.55，与 Rogers 1.549
  差 +0.3%、与 Dennis-Chang 1.522 差 +2.1%；Cd_p/Cp_front 一致到 0.2~1.3%。
- **远场距离很敏感**：R_out 10D（Rogers 同域）时 Cd=1.6659（+6%）、Cp_front=1.235。
  本算例远场是**反射型**（速度入口/定压出口），而 Rogers 用**特征型无反射** BC ⇒
  同域对比不宜直接，结论应以 25D 大域为准。



### 3. 无粘（Euler）解析解验证（用户提议，已验证可行）✅/✗
同一网格、同一套 BC，只把 `If_viscous=0`：**无粘圆柱有精确解** `Cp(θ)=1-4sin²θ`
（θ 自后驻点起），且 `Cd≡0`（d'Alembert）。结果（87844 步，res=1.0e-8）：

| θ | 178.9°(前驻点) | 160.9° | 133.9° | 88.9°(吸力峰) | 46.1° | 19.1° | 1.1°(后驻点) |
|---|---|---|---|---|---|---|---|
| ΔCp = 本文−解析 | +0.026 | +0.005 | +0.032 | +0.097 | +0.327 | −0.190 | **−0.611** |

`Cd_p=0.0393`（应 0）、远场动量平衡 `Cd=0.0192`（应 0）、上半升力 1.5665（应 1.6667，−6%）。

- ✅ **前/上半面（θ≳130°）Cp 与解析解一致到 0.5~3%**（前驻点 +2.6%、吸力峰 +0.10 ≈ 3.3%）
  ⇒ 壁面压力取值、压力通量、远场 BC、**对称 BC** 都被验证正确；半圆柱网格确实可以拿来
  做解析解验收；
- ✗ **后驻点区（θ≲20°）有明显缺陷**：数值解在尾部形成"死水区"（u≈0 但 Cp 平在 +0.39，
  解析应为 +1.0）⇒ 虚假阻力 0.02~0.04；
- ✗ **近壁总压偏低**：θ≈90° 径向 `H=p+q²/2` 远场 0.502，但**首层单元只有 0.185**，
  首层速度 1.808 vs 解析 1.973（−8%）⇒ 与粘性算例尾迹偏短（2.07~2.14 vs 2.29）一致；
- 单变量对照：**`AC_Flux=3`(AUSM+) 2000 步内 NaN 发散**（`inv_ausm/run_ausm.log`）；
  `AC_beta=10` 见 `inv_b10/run_b10.log`；`LS_Algorithm=1`(SIMPLE 同网格) **2 步内发散**
  （`simple_re40/try*.log`，与接口/多块无关，另案）。

### 4. 代码改动（向后兼容，已重编译）
`src/sub_lowspeed_ac.f90::ac_set_ghost_cell`：`BC_Wall` 且 `If_viscous=0`（无粘）时改为
**仅反转法向分量**（滑移壁），与可压求解器 `BC_Wall + If_viscous=0 → boundary_Symmetry_or_SlideWall`
的约定一致；`If_viscous=1` 分支逐位不变（粘性结果不受影响，本算例 g1/g2 均如此）。

### 5. 结论与待办
1. **半圆柱 + 对称边界是有效的验收构型**：粘性 Re=40 得到与 Rogers/Dennis-Chang 一致到
   0.3~2.1% 的 Cd（加密后），无粘可做逐点 Cp 解析对比；
2. 现有 AC 格式（MUSCL2 + AC-Rusanov）的**主要弱点在"强减速/回流区 + 近壁层"**：
   这不是多块/接口问题（本算例是单块），与 T 形管交汇处的工况同类 ⇒ T 形管发散很可能
   是"该弱点 + 多块调度/参数"叠加，值得按此顺序排查；
3. 待办：(i) 无粘圆柱上做 `AC_Recon=2`(WENO5+`AC_WenoBlend`)、限幅器、近壁重构（首层
   速度偏低）的单变量对照；(ii) 复现并定位 SIMPLE 路径 2 步发散（弧形边界 + 壁/对称角点）；
   (iii) `AC_Flux=3`(AUSM+) 的 NaN（改用 `AC_Flux=1` 才能跑）需在 AC 上下文里修稳；
   (iv) 若要与 Rogers 同域（10D）严格对比，需实现**特征型远场 BC**。

## 2026-09-17 —— 近壁/强减速区精度改进（Rusanov 耗散逐场分裂 + 壁面二阶压力 + 壁面切向单侧重构）；移除 AC_Flux=3（AUSM+）

承接 09-16：无粘半圆柱把 AC 格式的弱点量化到"**强减速/回流区 + 近壁层**"（尾驻点区 ΔCp=−0.61、
首层总压 0.185 vs 0.502）。按用户决定：**暂不做 T 形管**，先攻此项格式精度；并**删除
`AC_Flux=3`（AUSM+）**（AC 无物理声速，AUSM+ 不适合）。

### 1. 先诊断再改代码（避免"猜根因"）
靶子 = `cases/cylinder_re40_half/inviscid`（半圆柱 + 对称面，120×80，`t_end=1` 单块范式）。

| 单变量试验 | 收敛 | max\|ΔCp\| | RMS | 壁面 Cp(θ=1.1°) | ΔCp(88.9°) | Cd_p |
|---|---|---|---|---|---|---|
| MUSCL2 + van Leer（原基线） | 87840 步 | **0.6106** | 0.2129 | +0.388（解析 +0.999） | +0.097 | 0.0393 |
| MUSCL2 + **minmod**（更耗散） | 57062 步 | 0.7515 | 0.2873 | +0.247 | +0.187 | 0.0901 |
| **WENO5 + WenoBlend=0.1** | — | **NaN 发散** ✗ | — | — | — | — |
| WENO3 | 未收敛（42000 步 res 2.3e-4，慢于基线 1.0e-4） | — | — | — | — | — |

- **更耗散 ⇒ 更差**（minmod 残差降得更快——57k 步收敛——但 max\|ΔCp\| 由 0.61 恶化到 0.75）；
  **更少耗散（WENO5）⇒ 失稳 NaN** ⇒ 说明问题不是"耗散不足需要加高阶"，而是**耗散过量**；
- 逐点诊断（径向剖面 + 沿尾部对称线，脚本 `diag_rear.py`）：**0<θ<18° 的壁面 Cp 平台在 +0.39**
  （解析应从 +1.0 单调降到 0.62），**对称线后方 r=1.7R/2.1R 处 u=0.17/0.46 而解析 0.67/0.78**
  ⇒ 体后存在**数值"死水区"**（强减速区被过量数值耗散"粘住"），不是壁面单点处理；
- 吸力峰 ΔCp=+0.097 与曲壁离心项量级吻合：`(Δr₁/2)·(u_t²/R) = 0.0069·(2²/0.5) = 0.055`
  （ΔCp=2Δq≈0.11）⇒ **壁面压力取单元值丢掉了 ∂p/∂n**；
- 根因（改代码的依据）：`ac_flux_rusanov` 把 **`λ=max(|u_n|+c)` 统一施加到四个方程**，而 AC 特征值
  是 {u_n, u_n, u_n±c}：动量方程的耗散被放大 O(c/|u_n|)。β=AC_beta·U²=1 ⇒ c≈1.41，相当于给动量
  加了一个 ~√β·Δx 的人工粘性 —— 这正是"阻力主导/低马赫下 AC 比 SIMPLE 差、死水区、首层亏损"的同一根因。


### 2. 代码改动（5 项，均有开关、旧行为可精确复现）
`src/sub_lowspeed_ac.f90`（`+sub_modules.f90`/`sub_read_parameter.f90` 参数，`+sub_porous_ac.f90` 接线）：

1. **`AC_MomDiss`（默认 1）—— Rusanov 耗散逐场分裂**：质量方程保留 `λ=|u_n|+c`（共置压力场的
   耦合/防棋盘阻尼，相当于 RC 型阻尼），**动量方程改用 `λ=|u_n|`**；`=0` 逐位复现旧实现
   （控制组实测：iter 42000 时 res_q=1.0273098668023e-4 vs 旧 1.0273098669264e-4）。
2. **`AC_WallP`（默认 1）—— 壁面压力二阶重构** `q_wall=1.5q₁−0.5q₂`（新例程 `ac_wall_pressure` +
   `ac_wallface` 面映射，逐胞分辨部分边界）；平壁/边界层 `∂q/∂n=0` 时与旧式等价。
3. **`AC_WallRecon`（默认 1）—— 壁/对称面切向状态单侧二阶重构**：镜像 ghost 使切向 MUSCL 斜率
   恒为 0（`u_ghost=u_cell`）⇒ 壁面侧状态退化为一阶；改为单侧内点线性外推（`a_eff=2c−d`），
   法向分量仍用镜像 ghost（无穿透性与质量通量不变）；新增 `ac_tang_merge` 做切向/法向合成。
4. **粘性面距离按面法向投影**（`ac_viscous_res`/`dist_ac`）：斜网格上按指标线取距离会高估法向导数
   1/cosθ；正交网格上该投影恒等（槽道/方腔/圆柱逐位不变），仅斜/畸变网格受益。
   注：壁面 `d=|x₂−x₀|/2` 在节点镜像约定下恰好等于「2×壁到首层中心距」，故壁面剪切的二阶性原本正确。
5. **移除 `AC_Flux=3`（AUSM+）+ 新增 `ac_check_controls()`**：AC 无物理声速、AUSM+ 马赫分裂与
   LU-SGS 的 `c=√(u_n²+β)` 口径不一致、实现缺 AUSM+-up 的压力耗散项 p_u（历史：2000 步 NaN）。
   现 `AC_Flux` 仅 1(Rusanov)/2(Steger–Warming)；`AC_Flux/AC_Recon/AC_Limiter/AC_WenoBlend/
   AC_WallRecon/AC_WallP/AC_MomDiss` 全部越界告警并回落；启动打印一行完整 AC 控制量。
   `sub_porous_ac.f90` 同步调用 `ac_wall_pressure`/`ac_check_controls` 并分配新数组
   （否则多孔 AC 路径会读到未初始化/陈旧的壁面压力）。



### 3. 验收（同一二进制、单变量、多算例回归）
| 算例 | 旧（09-16 基线） | 本次（默认 `WallRecon=1/WallP=1/MomDiss=1` 及各单变量） |
|---|---|---|
| 无粘半圆柱 `inviscid`：max\|ΔCp\| / RMS | 0.6106 / 0.2129 | **0.5413 / 0.2072（WallP=1）**；WallRecon-only 0.6106（中性） |
| 同上：前驻点 ΔCp(178.9°) / 表面速度比 | +0.026 / 0.7850 | **−0.003 / 0.8071（WallP=1）** |
| 同上：尾驻点 ΔCp(θ=1.1°) / 吸力峰 ΔCp(88.9°) | −0.611 / +0.097 | **−0.541（改善） / +0.168（变差，记录在案）** |
| 同上：Cd_p（应 0）/ 收敛步数 | 0.0393 / 87840 步 | 0.0364 / 84969 步 |
| 槽道（单块）`channel_ac`：u_max（解析 1.5） | 1.49546（−0.303%） | **WallP 1.49572（−0.285%）→ +MomDiss(0.2) 1.49770（−0.153%）**，39377 步 res_q=1.0e-8 |
| 槽道（2 块）`channel_ac_2blk`：u_max / 界面 Σu 差 | 1.49439,1.49546 / 0.125% | **1.49465,1.49571 / 0.125%**，res_q=4.1e-11/1.1e-11 |
| 多孔塞 Darcy（AC-porous，40×20）u_bar（理想 7.925e-3） | 7.958e-3 | **7.905e-3**（−0.25% vs 理想），4968 步收敛 res_q=4.8e-9 |
| 粘性圆柱 Re=40（120×80）：Cd / Cd_p / Cd_f | 1.5706 / 1.0152 / 0.5553 | **1.5679 / 1.0128 / 0.5552（WallP=1）**，分离角 125.7°、尾迹 2.07D 不变 |
| 顶盖方腔 Re=100：u-RMS（Ghia）/ u_min | 0.02105 / −0.21412 | **0.02056 / −0.21647（WallP+MomDiss）**，15365 步收敛 |
| 收敛速率对照：槽道残差轨迹 2k/6k/10k/12k 步 | 8.481e-2 / 3.730e-2 / 7.377e-3 / 3.108e-3 | 8.474e-2 / 3.729e-2 / 7.374e-3 / 3.106e-3（**逐点几乎重合**） |

- **回归结论：零回归** —— 粘性圆柱 Cd 略优、方腔 u-RMS 略优、2 块 u_max 更接近解析，
  槽道/方腔的收敛轨迹逐点一致。
- **`AC_WallP=1` 净收益**：max\|ΔCp\| −11%、RMS −3%、前驻点几乎精确、Cd_p 更小、表面速度更接近解析；
  代价是**吸力峰**（θ≈80–90°）略差（+0.097→+0.168）——该处主因是首层速度亏损（−8%），与壁面压力无关。
  可用 `AC_WallP=0` 退回旧式。
- **`AC_WallRecon=1`**：无粘圆柱中性，槽道/2 块 u_max 略优；它修的是"壁面侧状态退化为一阶"这一确凿缺陷
  （对切向梯度大的壁面/角点最有意义，如 T 形管交汇、方管）。
- **`AC_MomDiss=1`（混合式，**非默认**）**：槽道残差轨迹逐点重合（无收敛代价）、槽道 u_max 由 1.49572
  进一步升到 **1.49770**（解析 1.5）、粘性圆柱 Cd 1.5679→**1.5589**；但无粘圆柱变差 ⇒ 默认仍取 0（见 §3.1）。
- **`AC_Flux=3` 移除**后 `ac_check_controls()` 的回落路径已实测（`cases/cylinder_re40_half/inv_ausm/control.ec`
  曾写 3；旧算例加载时打印告警并按 `AC_Flux=1` 运行）。

### 3.1 `AC_MomDiss` 逐场耗散分裂：纯式失稳、混合式有代价地改善粘性/内流、但损伤无粘解
| 变体（无粘半圆柱，`AC_WallP=1` 均开） | 稳定性 | max\|ΔCp\| | RMS | 尾驻点 ΔCp | Cd_p | 收敛步数 |
|---|---|---|---|---|---|---|
| `AC_MomDiss=0`（原式 `λ=|u_n|+c`，**默认**） | 稳 | **0.5413** | 0.2072 | −0.541 | 0.0364 | 84969 |
| `AC_MomDiss=1, AC_MomFrac=0.2` | 稳 | 0.8712 ✗ | 0.3984 | — | — | 87170 |
| `AC_MomDiss=1, AC_MomFrac=0.5` | 稳 | 0.6209 ✗ | 0.2451 | — | — | 89381 |
| `AC_MomDiss=2`（纯 `|u_n|`） | **发散** | — | — | — | — | 6000 步 0.15 → 16000 步 **0.40** |

同两项改动在**其它算例**上是正收益（同一二进制、单变量）：

| 算例 | `MomDiss=0`（默认） | `MomDiss=1, Frac=0.2` |
|---|---|---|
| 槽道 `channel_ac`：u_max 误差 / 剖面 RMS | −0.285% / 0.171% | **−0.153% / 0.130%**（39377 步，不变） |
| 粘性圆柱 Re=40：Cd / Cd_p | 1.5679 / 1.0128 | **1.5589 / 1.0066**（Rogers 1.549） |

- **纯 `|u_n|`（=2）不可用**：驻点/驻线处 u_n→0 ⇒ 动量耗散消失 ⇒ 共置压力-速度耦合失稳
  （无粘圆柱 6000/14000/16000 步 res_q = 0.15/0.24/0.40 单调上涨）。
- **混合式（=1）是"按需使用"的旋钮**：壁面主导/粘性内流（槽道、圆柱 Re=40）明显受益，
  但**无粘欧拉解反而变差**——那里唯一的耗散就是数值耗散，减小它会让共置格式在驻点/急减速区
  失去阻尼。因此**默认保持 `AC_MomDiss=0`**（既有验证结果不变），需要时按算例开启 1 并重新验收。
- `AC_MomFrac` 的最优值（0.1~0.5）与"死水区"指标（尾部 Cp 平台、对称线速度剖面、Cd_inviscid）
  的联动扫描仍未完成（见 §4 遗留）。



### 4. 本次未做 / 遗留
- `cases/2D_T_tube`（按用户决定本轮不做）；
- 首层切向速度亏损 −8%（吸力峰、Cl 偏差）——需要**曲线壁面/近壁切向重构**层面的改动，
  本次的壁压/切向单侧重构都不足以消除；
- 高阶重构（WENO5/WENO3）在共置 AC 格式下不稳/不成熟 ⇒ 暂不建议；
- 特征型远场 BC（R_out=10D 时 Cd 偏 +6%）。


## 2026-09-18 —— cases/fluid_solid 三算例（11/12/19，gas i+ ↔ 对侧 j-）分段交错（未提交）

在 `cases/fluid_solid` 网格（Block1 67×101×2 薄板 j=1 为对接面、Block2 101×201×2 通道
i=max 为对接面）上新建三个分段交错算例：**11 固体↔可压**、**12 低速↔可压**、
**19 多孔↔可压**（后两者 j=max 为冷却剂入口 G=2 kg/(m²·s)、300 K；Block2 = Ma=3/Re=5000/
T∞=800 K、壁面绝热）。既有耦合例程只支持“gas j- ↔ 对侧 j+”，故扩展了
`couple_highlow_fluid_face`、`couple_compressible_porous_blowing_face`、
`run_staggered_fluid_solid/highlow/fluid_porous`，并新增发汗壁 `wall_bound_blowing`
与界面诊断 `dbg_dump_interfaces`（`IF_Debug=1`）。三例短调度试跑均 EXIT=0、无 NaN：
case1 CHT 外层 `max|dT_w|` 499.9→0.094 K、`T_w≈799.6 K`；case2 外层度量正常、
LS-AC 待标定；case3 `max|q_w|` 5.3e6→3.9e6 W/m²、`T_w` 309→330 K。

> **细节 / 复现命令 / 待办清单见 `docs/进度_fluid_solid三算例.md`**（中断点记录）。

